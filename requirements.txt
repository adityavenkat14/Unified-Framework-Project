# Colab already ships torch/torchvision/CUDA -- don't reinstall those, just
# the extra packages the merged pipeline actually imports.
# (the original requirements.txt from ABS/WCA is a `conda create --file`
#  export, not pip-installable -- kept as requirements-conda-export.txt
#  for reference, but use this file with `pip install -r` instead.)

ftfy
regex
tqdm
pyyaml
opencv-python-headless
transformers
Pillow
requests
